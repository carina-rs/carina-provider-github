// Scaffold entrypoint. The WASI/native provider entrypoint is wired up
// in the follow-up resource PR (see docs/design.md).

fn main() {
    eprintln!(
        "carina-provider-github: scaffold only — provider implementation lands in a follow-up PR. \
         See docs/design.md."
    );
}
